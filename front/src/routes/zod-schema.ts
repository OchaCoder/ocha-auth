import { z } from "@builder.io/qwik-city"

// Action Output Universal Error
// export const outputActionErrorSchema = z.object({
//   success: z.literal(false),
//   payload: z.object({}), // Ensures it's an empty object
//   errorAction: z.object({
//     type: z.enum(["red", "green", "yellow", "close"]),
//     message: z.string(),
//   }),
// })
