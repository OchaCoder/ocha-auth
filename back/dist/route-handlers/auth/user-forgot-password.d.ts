import { FastifyInstance, FastifyReply, FastifyRequest } from "fastify";
export declare const userForgotPassowrd: (fastify: FastifyInstance) => (request: FastifyRequest, reply: FastifyReply) => void;
